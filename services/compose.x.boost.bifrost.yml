services:
  boost:
    environment:
      - HARBOR_BOOST_OPENAI_URL_BIFROST=${HARBOR_BIFROST_INTERNAL_URL}/v1
      - HARBOR_BOOST_OPENAI_KEY_BIFROST=${HARBOR_BIFROST_API_KEY}
    depends_on:
      bifrost:
        condition: service_healthy
