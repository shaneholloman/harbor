# This file is generated by seed-traefik.ts script,
# any updates will be overwritten.
services:
  unsloth-studio:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.unsloth-studio.rule=Host(`unsloth-studio.${HARBOR_TRAEFIK_DOMAIN}`)"
      - "traefik.http.services.unsloth-studio.loadbalancer.server.port=8000"
      - "traefik.http.routers.unsloth-studio.entrypoints=websecure"
      - "traefik.http.routers.unsloth-studio.tls=true"
      - "traefik.http.routers.unsloth-studio.service=unsloth-studio"

    networks:
      - traefik-public
